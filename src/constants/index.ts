import {
  Links,
  Style,
  QueryParams,
  TFormData,
  TDataInput,
  FormNames,
} from './types';
import { navLinks, inputs, countries, emailRegex } from './variables';

export {
  Links,
  Style,
  QueryParams,
  navLinks,
  inputs,
  countries,
  FormNames,
  emailRegex,
};
export type { TFormData, TDataInput };
