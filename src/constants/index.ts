import { Links, Style, QueryParams, TFormData } from './types';
import { navLinks, inputs, countries } from './variables';

export { Links, Style, QueryParams, navLinks, inputs, countries };
export type { TFormData };
