import {
  Links,
  Style,
  QueryParams,
  TFormData,
  TDataInput,
  FormNames,
} from './types';
import { navLinks, inputs, countries } from './variables';

export { Links, Style, QueryParams, navLinks, inputs, countries, FormNames };
export type { TFormData, TDataInput };
