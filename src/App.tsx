import { Component, ReactNode } from 'react';

export default class App extends Component {
  render(): ReactNode {
    return <h1 className="text-3xl font-bold text-red-500 underline text-center">Hello, {'lol'}!</h1>;
  }
}
